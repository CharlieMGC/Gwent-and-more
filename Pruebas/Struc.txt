struct Point {
    int x;
    int y;
}

fun Point createPoint(int x, int y) {
    Point p;
    p.x = x;
    p.y = y;
    return p;
}

fun void main() {
    Point p = createPoint(10, 20);
    print(p.x);
    print(p.y);
}
